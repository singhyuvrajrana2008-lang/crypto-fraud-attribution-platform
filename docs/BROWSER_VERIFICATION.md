# Browser Verification Notes

The integrated Vite site renders the supplied ChainTrace page at the root URL with the original title, hero, login section, live demo section, source styling, and Three.js canvas visible. The source copy `frontend/chaintrace.html` remains byte-for-byte identical to the supplied HTML; the Vite root entry adds only an external runtime module. The first proxied request required Vite `server.allowedHosts`; this was configured in `frontend/vite.config.js` and the page then loaded successfully.
The browser login form accepted controlled credentials and displayed the signed-in investigator session, unlocked the live demo form, and disabled unsupported chain options with “coming soon” labels. The page retained the supplied visual layout and Three.js scene.
After login, the live demo section displayed the unlocked wallet, Ethereum, fraud type, and amount controls. A valid Ethereum wallet was entered in the browser and unsupported chain options remained disabled as coming soon.
The browser submission controls were verified with a valid Ethereum wallet, selected fraud type, and amount. The source UI remains unchanged; the external runtime owns backend submission and result rendering.
The first live submission stayed pending because the browser was calling sandbox localhost directly. The integration was corrected to use relative `/api` requests with a Vite proxy to Flask; this is required for proxied browser deployments and avoids hard-coding localhost in the client.
After the proxy fix, the refreshed page loaded normally. The controlled login form again unlocked the session and preserved the supplied ChainTrace layout, ready for the live case request.
A clean browser instance served the integrated UI from the corrected Vite process, and controlled login again displayed the investigator session. The live demo form was ready for final API submission.
The clean-instance browser test exposed a test-sequencing issue rather than an application failure: filling login fields alone does not submit the form. The explicit Login button must be clicked before the protected demo can be submitted.
The integrated form correctly enforced the required wallet field during browser testing; a submission with only default amount and no wallet was blocked by native validation. This confirms the supplied UI’s required-field behavior remains active.
The final browser submission succeeded through the Vite proxy and Flask backend. The source result panel rendered a backend case reference, ₹240,000 amount, critical risk score 100/100, structured priority factors, risk indicators, simulated fund flow, Demo Exchange association, and related complaint count. The original source UI styles and layout were retained.
